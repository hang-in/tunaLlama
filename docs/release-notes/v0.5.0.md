# v0.5.0 - Cross-environment verified (usable dogfooding) (2026-05-11)

> **Note**: 초기 v0.5.0 은 "Production Release" 로 표시됐었으나, 외부
> 사용자 재현성과 organic dogfooding 측정 자산은 수집 중이라는 점을 더
> 정확히 반영하기 위해 **usable dogfooding release** 로 표현을 정리.
> 기능 변경 없음 - 명명 정리만.

## 한 줄

**Claude Code + Codex CLI 한 레포 / 두 환경 MCP tool 작동 검증**.
External audit must-fix + 권장 처리 완료, INSTALL.md (agent-driven) +
CONTRIBUTING + 측정자산 archive 정리, **first cross-environment-verified
release**.

## v0.4.0 → v0.5.0

| 영역 | 변화 |
|---|---|
| **Codex CLI 호환** | Phase 8 - subagent TOML + 사용자 환경 실측 검증 |
| **release-readiness audit** | must-fix 3개 + 권장 5개 처리 |
| **온보딩** | INSTALL.md (agent 위임 가능) + README 5분 가이드 / FAQ |
| **기여 인프라** | CONTRIBUTING.md + ISSUE 템플릿 + CI gate |
| **에러 메시지** | 영문 → 한국어 (사용자 1순위) |
| **측정자산** | abandoned (Phase 4-4 saturate + Phase 5-2D MMR) archive |

## Codex CLI 실측 (2026-05-11)

사용자 환경 (Codex CLI 0.128.0) 에서 검증:

```
codex plugin marketplace add /path/to/tunaLlama  → "Added marketplace tunallama-local"
codex mcp add tunallama -- /abs/python -m plugin.mcp_server  → "Added global MCP server"
```

Codex 세션에서:
- ✓ 13 도구 list (`tuna_load_memory`, `tuna_recall`, `tuna_generate_code` 등)
- ✓ `tuna_load_memory()` 정상 (state.md 빈 응답)
- ✓ `tuna_recall("test")` - 54 매칭, 상위 5건 반환
- ✓ `tuna_generate_code` - Ollama Cloud 호출 + 한국어 코드 반환

**핵심 발견 (외부 조사 정정)**: Codex 0.128.0 의 `codex plugin marketplace`
와 `codex mcp` 는 **별도 메커니즘**. 두 명령 모두 실행 필요. INSTALL.md
4-B 섹션에 명시.

## audit 처리

### must-fix (배포 차단 해결)

1. `plugin/.mcp.json` 절대경로 → portable (`python` + `${CLAUDE_PLUGIN_ROOT}/..`)
2. ruff 43 errors → 0 (F401 / F541 / F821 / F841 / E702)
3. `.env.example` 4 env vars 추가

### 권장 (release 정리)

- abandoned mmr docstring 명시
- `dev_review_loop` 93줄 → `_run_review_iteration()` 추출
- `init_cmd` broad except → 구체 예외
- `.github/workflows/ci.yml` 추가
- `__all__` 명시 (이미 보유)

## 새 자산

| 파일 | 내용 |
|---|---|
| [INSTALL.md](../../INSTALL.md) | agent-driven 설치 가이드 (한 줄 위임 가능) |
| [CONTRIBUTING.md](../../CONTRIBUTING.md) | 기여 가이드 |
| `.github/ISSUE_TEMPLATE/` | bug / feature 템플릿 |
| `.github/workflows/ci.yml` | ruff + pytest gate |
| `plugin/agents/tuna-developer.toml` | Codex 호환 subagent (Claude `.md` 와 동시 보존) |
| `tests/integration/archive/` | abandoned 측정 자산 보존 |
| `docs/measurements/phase7-context-boost.md` | 3 모델 측정 종합 |
| `docs/measurements/phase7-mcp-audit.md` | MCP 도구 system prompt size |

## 측정 자산 종합 (v0.5.0 기준)

| 항목 | 값 |
|---|---|
| 검색 winner | HyDE hybrid (P@1 0.92 / σR@5 0.14) |
| context boost (3 model) | +0.58 ~ +0.64 |
| corpus 효과 | 102 → 524 record σR@5 -0.05 |
| MCP 도구 system prompt | ~1633 tokens (3.5 char/token 추정) |
| 단위/플러그인 tests | 475 passing |
| Coverage | 90% |
| dogfooding rounds | 17 (Round 16 의 channel switch 가 standalone-toy 패턴 깸) |

## 한계 (정직, 변경 없음)

- 사용 한도 절약은 체감 데이터 (Anthropic/OpenAI 한도 계산식 비공개)
- 검색 측정값 합성 시드 기반 - 실 사용자 워크플로우 검증은 별개
- organic dogfooding 측정 부재 (Round 17+ 부터 재개 예정)
- MCP 도구 system prompt 1633 tokens 비용 (prompt caching 적용 여부에
  따라 실 비용 변동)
- Codex subagent (`tuna-developer.toml`) 자동 인식 / `tunallama://memory/state`
  resource auto-attach 는 Codex 0.128.0 에서 미검증

## v0.6.0 후보

- organic dogfooding metrics 수집 (Round 17+)
- Codex subagent auto-load 검증
- state.md recall probe 자동화
- diff-based learning + LM Studio 작은 로컬 모델 실 통합
- 1k-10k corpus 측정

## 라이선스

MIT - 변경 없음. 이슈 / PR 환영.
