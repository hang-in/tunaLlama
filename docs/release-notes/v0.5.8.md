# v0.5.8 - 1k LOPO corpus scaling + README tone 정리 (2026-05-11)

## 한 줄

검색 측정 corpus 524 → 984 record 확장 결과 + README 의 production 톤
표현 정리 (usable dogfooding 으로 reframe).

## Phase 5-E - 984 record LOPO 측정

```
path     P@1   R@5   MRR  NDCG@5  σP@1  σR@5
BM25    0.53  0.31  0.62   0.36   0.50  0.25
vec     0.75  0.55  0.82   0.60   0.43  0.30
hybrid  0.63  0.42  0.73   0.47   0.48  0.27
rerank  0.77  0.59  0.83   0.63   0.42  0.31
```

vs Phase 5-1 (524 record):
- 전 path P@1 +0.10~0.13 / R@5 +0.08~0.16 향상.
- rerank R@5 +0.16 (0.43 → 0.59). corpus 클수록 cross-encoder 우위.
- σ ∝ 1/√N 가설 거짓: σR@5 0.26 → 0.31 오히려 증가
  (NEW_GROUPS_60_v2 도메인 추가가 corpus 효과 상쇄).

자세한 결과: `docs/measurements/phase5e-corpus-scaling.md`.

## Phase 5-D - rerank candidate_pool sweep

```
pool  P@1   R@5   MRR  NDCG@5
20   0.66  0.43  0.75   0.49
50   0.66  0.44  0.76   0.49
100  0.67  0.43  0.76   0.49
```

pool >20 효과 미미. default 20 유지.

## README 톤 정리 (외부 검토 반영)

1. Status badge: `production` → `usable beta`. 본문 "v0.5.0 production
   release" → "v0.5.x usable dogfooding release".
2. 첫 문단 직후 포지셔닝 명시: tunaLlama 는 **MCP 기반 delegation
   runtime** (prompt seed / AGENTS.md 템플릿 아님).
3. context boost +0.58~+0.64 앞에 "합성 시드 기반" 자격 명시.
4. "Claude/Codex 가 자동으로..." → "Architect 가 호출 가능, 첫 사용엔
   명시 호출 권장" (explicit invocation recommended).
5. MCP token overhead reframe: 인지된 리스크 → **의도된 trade-off /
   affordance 비용**.
6. "왜 프롬프트 시드가 아닌가" 섹션 신규.
7. 한계 섹션 6 카테고리 재구성 (cost / measurement / MCP / local LLM
   / search / state.md).
8. 한도 절약 표현: "정량 보장 X" 명시 + "위임 구조 제공" positive
   framing.

기존 GitHub release notes 의 production 표현 정리:
- v0.5.0 title: "Production Release" → "Cross-environment verified
  (usable dogfooding)".
- v0.5.6/v0.5.7/v0.5.1 의 production 표현 정리.

## 새 자산

| 파일 | 내용 |
|---|---|
| `docs/measurements/phase5e-corpus-scaling.md` | Phase 5-D + 5-E 측정 |
| `tests/integration/seeds/extended_1k.py` | 984 record seed (132 task × 6 + 192 noise) |
| `tests/integration/test_search_quality_lopo_1k.py` | 1k LOPO test |
| `tests/integration/test_search_quality_lopo_500.py` | rerank pool sweep test 추가 |

## 한계 (변경 없음)

- σ ∝ 1/√N 외부 가설 검증 실패. corpus 크기뿐 아니라 group 난이도 분포가
  σ 결정.
- HyDE / KURE / Adaptive path 1k 측정은 cloud 호출 비용 큰 별 단계.
- per-domain σ 분해는 sample size 작아 추가 측정 필요.
