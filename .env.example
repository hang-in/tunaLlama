# tunaLlama .env 예시 — `.env`로 복사 후 값 채우기.
# config.toml 의 `api_key_env = "..."` 가 가리키는 환경변수만 의미가 있다.

# Ollama Cloud (config.toml 의 [llm.ollama_cloud].api_key_env 와 일치시킬 것)
# OLLAMA_CLOUD_API_KEY=

# LM Studio (로컬은 보통 무의미)
# LMSTUDIO_API_KEY=
